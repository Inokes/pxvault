# pxvault
this is a personal website that instead of
the usual html pages you use markdown instead.
that's it.

i used to make my own markdown transpiler, until then
i hadn't yet discovered the node marked parser.

